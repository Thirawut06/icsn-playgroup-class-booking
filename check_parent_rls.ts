import { createClient } from '@supabase/supabase-js'; import * as dotenv from 'dotenv'; dotenv.config({ path: '.env.local' }); const supabase = createClient(process.env.NEXT_PUBLIC_SUPABASE_URL || '', process.env.NEXT_PUBLIC_SUPABASE_ANON_KEY || ''); async function check() { const { data: { user } } = await supabase.auth.signInWithPassword({ email: 'test_rls@example.com', password: 'password' }); if (!user) { await supabase.auth.signUp({ email: 'test_rls@example.com', password: 'password' }); await supabase.auth.signInWithPassword({ email: 'test_rls@example.com', password: 'password' }); } const { data, error } = await supabase.from('system_settings').select('*'); console.log('RLS Data:', JSON.stringify(data)); console.log('RLS Error:', error); } check();
